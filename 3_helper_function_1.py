

class Item:
    @staticmethod
    def is_integer():
        '''
        similar to outside function; they are kind of similar to outisde helper functions but this is somehow similar to this class

        '''

    @classmethod
    def instantiate_from_something(cls):
        '''
        
        This basically used to instantiate projects
        
        '''




item1 = Item()
item1.is_integer(5)
item1.instantiate_from_something()

# you can call these from the instances, though it is rare and we usually call these from the calss method
